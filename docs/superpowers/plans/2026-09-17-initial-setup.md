# Initial setup and enforced authentication — 0.4.9

User-directed version lock: remain on 0.4.9 until Troy explicitly authorizes a change.

1. Add regression tests for one-time setup, required credentials/API key, transactional settings, private API/session/key enforcement, and cross-origin mutation rejection.
2. Extend existing setup endpoints with random key generation and atomically enable UI/API protection. Restore server authentication and require a browser session for account changes.
3. Replace bypassed auth boot with welcome/login states, password confirmation, generated key copy/regeneration, errors and Settings redirect. Preserve account/logout behavior and protect app content while signed out.
4. Exercise browser setup/login/logout and server regressions, then run full suites. Update release documentation and container smoke setup without changing version.

Setup uses the first administrator as its completion marker. Existing configured administrators retain credentials. A first-run key candidate is generated by Python secrets and only stored when setup succeeds. Setup cannot replace an existing administrator. No credentials go in URL/local storage. Initial setup is a first-visitor claim flow on the installation URL; cross-origin mutations are rejected.

Verification: 546 Python tests passed, dashboard/settings/auth browser checks passed, lint and JS syntax checks passed. Independent review findings (proxy authority and custom username preservation) fixed. Container checks exercise setup, anonymous denial, API key access, logout/login, backups and restart persistence through nginx at UID 568 and 1000. No release published.
