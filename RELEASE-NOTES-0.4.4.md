# ScarletX 0.4.4

Corrects ScarletX branding by shipping the exact approved horizontal logo artwork and exact approved ribbon-only favicon/app icon instead of reconstructed SVG approximations. Adds cache-busted UI references so browsers load the corrected assets immediately, while preserving the approved dashboard banner, navigation icons, configuration, and TrueNAS deployment behavior.
